#include <iostream>

using namespace std;

int main() {
    int contador;

    // Contar de 1 a 10 usando for
    for (contador = 1; contador <= 10; ++contador) {
        cout << contador << endl;
    }

    // Contar de 10 a 1 usando for (comentado)
    /*
    for (contador = 10; contador > 0; --contador) {
        cout << contador << endl;
    }
    */

    return 0;
}
