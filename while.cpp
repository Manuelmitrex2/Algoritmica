#include <iostream>

using namespace std;

int main() {
    int valor = 1;

    // Ciclo while que incrementa de 1 a 10
    while (valor <= 10) {
        cout << valor << endl;
        valor++;
    }

    // Ciclo while para contar hacia atrás (comentado)
    /*
    valor = 10;
    while (valor >= 0) {
        cout << valor << endl;
        valor--;
    }
    */

    return 0;
}
